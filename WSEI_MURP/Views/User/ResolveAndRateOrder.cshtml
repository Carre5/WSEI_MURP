@model WSEI_MURP.Models.ViewModels.RateOrderViewModel
@{
    ViewData["Title"] = "Resolve Order";
}

<h1>Resolve Order</h1>

<form asp-action="RateOrder">

    <div class="form-group" style="display:none">
        <label asp-for="OrderID" class="control-label"></label>
        <input asp-for="OrderID" value="@ViewBag.Order.OrderID" />
        <span asp-validation-for="OrderID" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="OrderRating" class="control-label"></label>
        <select asp-for="OrderRating"
                asp-items="@(new SelectList(ViewBag.Ratings))" class="form-control">
        </select>
        <span asp-validation-for="OrderRating" class="text-danger"></span>
    </div>
    <div class="form-group">
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>
</form>